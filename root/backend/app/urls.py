from django.urls import path
from . import views

urlpatterns = [
    # path("notes/", views.NoteCreate.as_view(), name="note-create"),
    # path("notes/delete/<int:pk>", views.NoteDelete.as_view(), name="note-delete"),
]
